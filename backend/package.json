{
  "name": "moroz-art-backend",
  "version": "1.0.0",
  "description": "Combined backend API for Moroz Art marketplace with Stripe integration",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-deps": "npm install",
    "check-config": "node -e \"require('dotenv').config(); console.log('Environment variables loaded successfully');\"",
    "webhook-test": "node -e \"console.log('Webhook endpoint: http://localhost:' + (process.env.PORT || 3001) + '/api/webhooks/stripe');\""
  },

  "keywords": [
    "stripe",
    "connect",
    "marketplace",
    "art",
    "payments",
    "firebase"
  ],
  "author": "Moroz Art",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^4.21.2",
    "express-rate-limit": "^7.1.5",
    "firebase": "^10.14.1",
    "firebase-admin": "^12.0.0",
    "helmet": "^7.1.0",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.9.7",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "axios": "^1.11.0",
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}